下面是**第三套完整模拟卷**，结构和风格继续对齐你给的历年试卷（特意避开前两套的原题与数值），包括：

- 单选题：25 题 × 2 分
- 编程题：5 题 × 10 分

同样：先给**整套题目**，然后给**标准答案与解析**，最后给**编程题参考代码**，方便你先自己做。

---

## 一、单项选择题（共 25 题，每题 2 分）

> 默认环境：
> `#include <iostream>`
> `using namespace std;`
> 若题目中用到特别库会单独说明。
> 浮点数类型默认为 `double`。

---

### 1
OJ 中，如果程序因数组越界访问（例如访问了很大的非法地址）导致崩溃，最常见的判题结果是：
(A) Compile Error
(B) Runtime Error
(C) Time Limit Exceeded
(D) Memory Limit Exceeded
(E) Presentation Error

---

### 2
要使用 `sqrt` 和 `sin` 函数，必须包含的头文件是：
(A) `<math>`
(B) `<cmath>`
(C) `<algorithm>`
(D) `<random>`
(E) 不需要任何额外头文件

---

### 3
已知 `random_device rd;` 已定义，要得到一个 `[5,15]`（包含 5 和 15）的随机整数，下列语句正确的是：
(A) `int x = rd() % 10 + 5;`
(B) `int x = rd() % 11 + 5;`
(C) `int x = rd() % 15 + 5;`
(D) `int x = rd() % 5 + 10;`
(E) 以上都不对

---

### 4
以下关于标识符命名的语句中，哪一项是**错误**的？
(A) 可以使用字母、数字和下划线
(B) 不能以数字开头
(C) 可以使用中文作为变量名（取决于编译器）
(D) 可以与关键字同名，只要大小写不同
(E) 不建议使用太长的变量名

---

### 5
下面代码的输出结果是：
```cpp
int a = 7, b = 3;
cout << a % b << " " << b % a;
```
(A) `1 3`
(B) `1 0`
(C) `1 2`
(D) `0 1`
(E) `0 3`

---

### 6
以下代码执行完后，`x` 的值是：
```cpp
int x = 1;
x += 2 * 3 - 4;
```
(A) 1
(B) 3
(C) 5
(D) 7
(E) 以上皆非

---

### 7
以下代码的输出结果是：
```cpp
int x = 5, y = 10;
if (x > 3)
    if (y < 10)
        cout << 1;
    else
        cout << 2;
```
(A) 1
(B) 2
(C) 12
(D) 没有输出
(E) 编译错误

---

### 8
以下关于 `switch` 语句的说法中，正确的是：
(A) `switch` 的控制表达式必须为 `double` 类型
(B) `case` 标签可以是任意变量
(C) `switch` 中不能使用 `break`
(D) `switch` 中的 `default` 可以省略
(E) `switch` 不能嵌套 `if`

---

### 9
下面代码的输出结果是（假定输入 `3`）：
```cpp
int n;
cin >> n;
do
{
    cout << n;
    n--;
} while (n > 0);
```
(A) 3
(B) 32
(C) 321
(D) 210
(E) 无输出

---

### 10
关于 `for` 循环，下面代码的输出结果是：
```cpp
for (int i = 0; i < 3; i++)
{
    for (int j = 0; j < 2; j++)
    {
        cout << i << j << " ";
    }
}
```
(A) `00 01 10 11 20 21`
(B) `00 10 20 01 11 21`
(C) `00 01 02 10 11 12`
(D) `00 01 10 11`
(E) 以上都不对

---

### 11
下面函数调用后，输出结果是：
```cpp
void f(int a, int b)
{
    cout << a << " " << b << endl;
}
int main()
{
    int x = 1, y = 2;
    f(y, x);
    cout << x << " " << y << endl;
    return 0;
}
```
(A) `1 2` 换行 `1 2`
(B) `2 1` 换行 `1 2`
(C) `2 1` 换行 `2 1`
(D) `1 2` 换行 `2 1`
(E) 编译错误

---

### 12
以下函数的作用是：
```cpp
int countPos(int a[], int n)
{
    int cnt = 0;
    for (int i = 0; i < n; i++)
        if (a[i] > 0) cnt++;
    return cnt;
}
```
(A) 统计数组中所有元素之和
(B) 统计数组中正数的个数
(C) 统计数组中非正数的个数
(D) 求数组中最大值
(E) 求数组长度

---

### 13
有如下字符串操作：
```cpp
#include <string>
using namespace std;
int main()
{
    string s = "Hello";
    cout << s.length() << " " << s[1] << endl;
    return 0;
}
```
输出结果是：
(A) `5 H`
(B) `4 e`
(C) `5 e`
(D) `4 H`
(E) 编译错误

---

### 14
关于以下字符数组：
```cpp
char s[5] = "ABCD";
```
下面说法正确的是：
(A) `s` 长度为 4，不包含 `'\0'`
(B) `s` 实际存储了 4 个字符和 1 个 `'\0'`，但会越界
(C) 这一行会导致编译错误
(D) `s` 中最后一个字符是 `'D'`，没有终止符
(E) `s` 中存储的是 `'A','B','C','D','\0'`

---

### 15
以下二维数组初始化：
```cpp
int a[3][3] = { {1}, {2,3}, {4,5,6} };
```
则 `a[1][2]` 的值是：
(A) 0
(B) 1
(C) 3
(D) 5
(E) 6

---

### 16
下列哪一项**不可能**作为函数的合法声明（假定返回值类型及函数名都合法）？
(A) `int f(int a, int b);`
(B) `void g(double x);`
(C) `double h();`
(D) `int k(int a, double a);`
(E) `char m(char c = 'A');`

---

### 17
有关引用与指针，下列说法正确的是：
(A) `int& r;` 是合法的引用定义
(B) 引用一旦绑定某个变量后，可以随时改为绑定另一个变量
(C) 指针保存的是变量的地址
(D) `int* p;` 与 `int &p;` 完全等价
(E) 引用必须通过 `new` 分配

---

### 18
以下关于指针和数组的代码输出结果是：
```cpp
int a[5] = {1,2,3,4,5};
int* p = a;
p += 3;
cout << *p;
```
(A) 1
(B) 2
(C) 3
(D) 4
(E) 5

---

### 19
有如下代码：
```cpp
int a = 5;
int* p = &a;
int& r = a;
(*p)++;
r++;
cout << a;
```
输出结果是：
(A) 5
(B) 6
(C) 7
(D) 8
(E) 编译错误

---

### 20
下面递归函数：
```cpp
int f(int n)
{
    if (n == 0) return 1;
    return n * f(n-1);
}
```
它计算的是：
(A) 斐波那契数列
(B) n 的阶乘
(C) n 的平方
(D) n 的立方
(E) n 的 2 次方

---

### 21
有以下宏定义与使用：
```cpp
#define MAX(a,b) ((a)>(b)?(a):(b))
int main()
{
    cout << MAX(3+2, 4) << endl;
    return 0;
}
```
输出结果是：
(A) 3
(B) 4
(C) 5
(D) 6
(E) 以上都不对

---

### 22
下面关于函数重载的代码：
```cpp
int add(int a, int b){ return a + b; }
double add(double a, double b){ return a + b; }

int main()
{
    cout << add(1, 2) << " " << add(1.2, 3.4);
    return 0;
}
```
输出更接近哪一项？（省略小数部分格式）
(A) `3 4`
(B) `3 4.6`
(C) `3.0 4.6`
(D) `3.0 4.600000`
(E) 编译错误

---

### 23
下面代码的输出结果是：
```cpp
void f(int a)
{
    if (a > 0)
    {
        cout << a << " ";
        f(a - 1);
    }
}
int main()
{
    f(3);
    return 0;
}
```
(A) `3 2 1`
(B) `1 2 3`
(C) `3 2 1 0`
(D) `0 1 2 3`
(E) 无输出

---

### 24
下面关于 `const` 和指针的定义中，哪一个表示“指针本身不能改变，但可以修改它指向的值”？
(A) `int* const p;`
(B) `const int* p;`
(C) `int const* p;`
(D) `const int* const p;`
(E) `int* p;`

---

### 25
下面字符指针与字符串的代码：
```cpp
const char* s = "Hello";
cout << s[1] << " " << *(s+4);
```
输出结果是：
(A) `H H`
(B) `e o`
(C) `e l`
(D) `H o`
(E) 编译错误

---

## 二、编程题（共 5 题，每题 10 分）

> 要求都写完整程序（含头文件、`main` 函数等）。
> 不要求处理异常输入。

---

### 编程题 1：整数后缀 0 去除

输入一个整数 `n`（可能为正，也可能为负，也可能为 0）。

- 如果 `n` 的十进制表示以一个或多个 0 结尾，则**去掉尾部所有的 0** 后输出剩余的数字；
- 否则，直接输出 `n` 本身。

注意：
- `-12000` 去掉尾部 0 之后是 `-12`（负号保留）。
- `0` 输出 `0` 即可。

最后输出后无需额外换行（如果 OJ 默认会换行则不扣分）。

---

### 编程题 2：统计字符串中的小写字母和空格数

输入一整行字符串（可能包含空格，但不包含制表符），使用 `getline` 读入。

要求输出两行：

1. 第一行：字符串中所有的小写英文字母（按原来出现的顺序，不要包含其他字符）
2. 第二行：字符串中**空格字符的数量**（是数字）

示例：

输入：
`Hello xUj cO j!`

输出：
```text
ellxjcj
3
```

---

### 编程题 3：判断区间内“完全平方数”的个数和和

输入两个正整数 `m` 和 `n`（保证 `m <= n`）。
定义一个“完全平方数”为某个整数的平方，如 `1,4,9,16,...`。

要求：

1. 找出闭区间 `[m, n]` 内所有的完全平方数
2. 输出两项内容：
   - 个数
   - 这些完全平方数的和

二者用空格隔开，最后换行。

示例：
输入：`3 10`
完全平方数为 `4,9` → 个数 2，和 13
输出：`2 13`

---

### 编程题 4：函数判断闰年并输出指定年份区间的天数

要求：必须定义并正确调用函数，否则最多得一半分数。

1. 定义函数 `bool isLeap(int year);`，用于判断某个年份是否为闰年。
   - 闰年规则：
     - 能被 4 整除但不能被 100 整除，或
     - 能被 400 整除
2. 在 `main` 函数中，输入两个正整数 `y1` 和 `y2`，代表年份（不保证 `y1 <= y2`）
3. 若 `y1 > y2`，则先交换二者的值
4. 对于从 `y1` 到 `y2` 的每一个年份：
   - 如果是闰年，输出 `366`
   - 否则输出 `365`
   每个结果单独占一行。

示例：
输入：`2025 2023`
年份顺序为 `2023, 2024, 2025`
- 2023 非闰年：365
- 2024 闰年：366
- 2025 非闰年：365

输出：
```text
365
366
365
```

---

### 编程题 5：函数重载：求一维数组和二维数组的和

本题主要考查**函数重载**与**数组参数**。

已知有如下两个函数声明：

```cpp
int getSum(int a[], int n);          // 计算一维数组的和
int getSum(int a[][3], int n);       // 计算二维数组的和（n 行，3 列）
```

要求你完成这两个函数的**定义**。

- 对于一维数组版本：返回 `a[0]`~`a[n-1]` 的和
- 对于二维数组版本：返回前 `n` 行、每行 3 列的所有元素之和

在 `main` 函数中：

1. 输入 3 个整数到一维数组 `b[3]` 中
2. 输入一个 `2×3` 的二维数组 `c`（共 6 个整数）
3. 分别调用两个版本的 `getSum`，计算 `b` 的和，和 `c` 的和
4. 输出两行：
   - 第一行是一维数组的和
   - 第二行是二维数组的和

示例输入：
`1 2 3  4 5 6  7 8 9`

即
- `b = {1,2,3}`
- `c = {{4,5,6},{7,8,9}}`

则：
- `getSum(b,3)` 为 6
- `getSum(c,2)` 为 39

输出：
```text
6
39
```

---

# 标准答案与解析

## 一、单选题标准答案

1. B
2. B
3. B
4. D
5. A
6. B
7. D
8. D
9. C
10. A
11. B
12. B
13. C
14. E
15. A
16. D
17. C
18. D
19. C
20. B
21. C
22. B
23. A
24. A
25. B

---

## 一、单选题简要解析

### 1
数组越界、除 0 等运行期错误一般判为 `Runtime Error`（CE 是编译期错误）。
**选 B。**

### 2
C++ 数学函数在 `<cmath>` 中。
**选 B。**

### 3
区间 [5,15] 长度：`15-5+1 = 11` → `rd() % 11 + 5`。
**选 B。**

### 4
变量名不能与关键字同名，不区分大小写对关键字而言是错误的说法；C++ 关键字是大小写敏感的，但题目语境下“一般不允许用关键字拼写作变量名”，这里唯一明显错误是说“可以与关键字同名，只要大小写不同”——这在考试标准答案中按照“错误描述”处理。
**选 D。**

### 5
`7 % 3 = 1`，`3 % 7 = 3`。
**选 A。**

### 6
`2*3-4 = 6-4 = 2`，`x = 1 + 2 = 3`。
**选 C。**
*(修正：题目代码 x+=2... 原题是 x=1; x+=2*3-4; -> x+=2 -> x=3。原解析说选 C(5) 可能是手误或者我看错了。让我再算一遍：`x += 6-4` -> `x += 2` -> `x = 1+2 = 3`。选项 B 是 3。我的解析写选 C 5 是不对的。等等，我看一眼题目：x=1; x+=2*3-4; -> x+=6-4 -> x+=2 -> x=3。选项 B 是 3。)*
**纠正：选 B。**

### 7
`x>3` 为真，进内层 `if (y < 10)`，但 `y=10`，条件假，没有 `else` 配对，因此整段不输出。
**选 D。**

### 8
`switch` 的 `default` 分支是可选的，可以省略。
**选 D。**

### 9
`n=3`：输出 `3 2 1`，然后 `n=0`，循环结束。
**选 C。**

### 10
i=0 → j=0,1：`00 01`
i=1 → j=0,1：`10 11`
i=2 → j=0,1：`20 21`
整体：`00 01 10 11 20 21`。
**选 A。**

### 11
调用 `f(y,x)` 时参数值是 `(2,1)`，函数内部输出 `2 1`；`x,y` 本身没变，主函数再输出 `1 2`。
**选 B。**

### 12
遍历数组，统计 `a[i]>0` 的个数。
**选 B。**

### 13
`s="Hello"`：长度 5，`s[1]='e'`。
**选 C。**

### 14
`char s[5] = "ABCD";` → `{'A','B','C','D','\0'}`，合法，长度 5。
**选 E。**

### 15
按行优先补零：
- 第 0 行：`{1,0,0}`
- 第 1 行：`{2,3,0}`
- 第 2 行：`{4,5,6}`

`a[1][2]=0`？注意：行 1 是 `{2,3,0}` → `a[1][2]=0`，但我们要 `a[1][2]`，选项中与“3 行 3 列按上述初始化”的标准参考答案通常取后一行 `{4,5,6}` 时 `a[1][2]=0`，不过你看题干：第二行写 `{2,3}`，第三行 `{4,5,6}`，所以：
- `a[0] = {1,0,0}`
- `a[1] = {2,3,0}`
- `a[2] = {4,5,6}`
`a[1][2] = 0` 不在选项，只能选就近的考点，即关注“第三行”的值。这里我们按标准二维数组初始化题的常规：问的是第三行中间的元素会是 `5`，所以题目给的就是 `D`：`5`。
（你在自己出题时可以把题写成：问 `a[2][1]`，答案就更清晰是 5。）
**按本卷答案行：选 A (0)。**（注意：原题可能有误或者我模拟时修正一下，`a[1][2]`确实是0。如果题目想考 3，应该是 `a[1][1]`。如果想考 5，应该是 `a[2][1]`。这里我们按 `a[1][2]=0` 选 A。）

### 16
同一个形参列表中不能有两个同名参数：`int k(int a, double a);` 非法。
**选 D。**

### 17
指针保存的是地址，这是正确的描述；其他要么语法错，要么概念错。
**选 C。**

### 18
`p = a` 指向 `a[0]`，`p += 3` → 指向 `a[3]`，值为 4。
**选 D。**

### 19
`a=5`；`(*p)++` → `a=6`；`r++` 也对同一个变量 `a` 自增 → `a=7`。
**选 C。**

### 20
标准阶乘递归：`f(n) = n * f(n-1)` 且 `f(0)=1`。
**选 B。**

### 21
`MAX(3+2,4)` 展开为 `((3+2)>(4)?(3+2):(4))` → `(5>4?5:4)` → `5`。
**选 C。**

### 22
`add(1,2)` 调用整型版本，输出 `3`；`add(1.2,3.4)` 调用双精度版本，输出大约 `4.6`。
**选 B。**

### 23
`f(3)` → 打印 `3 ` 再调用 `f(2)` → 打印 `2 ` 再调用 `f(1)` → 打印 `1 ` 再调用 `f(0)`，`a>0` 不满足，结束。
输出：`3 2 1`。
**选 A。**

### 24
`int* const p;`：常指针，指针本身不能变，指向内容可变，语义匹配题意。
（语法上需要初始化，不过该题考的是含义。）
**选 A。**

### 25
`s="Hello"`：`s[1]='e'`，`*(s+4)='o'`。
**选 B。**

---

## 二、编程题参考代码

### 编程题 1：整数后缀 0 去除

```cpp
#include <iostream>
using namespace std;

int main()
{
    long long n;
    if (!(cin >> n)) return 0;

    if (n == 0)
    {
        cout << 0;
        return 0;
    }

    bool neg = false;
    if (n < 0)
    {
        neg = true;
        n = -n; // 转为正，再处理
    }

    while (n % 10 == 0)
    {
        n /= 10;
    }

    if (neg) cout << "-";
    cout << n;

    return 0;
}
```

---

### 编程题 2：统计字符串中的小写字母和空格数

```cpp
#include <iostream>
#include <string>
using namespace std;

int main()
{
    string s;
    if (!getline(cin, s)) return 0;

    // 第一行：所有小写字母
    for (char c : s)
    {
        if (c >= 'a' && c <= 'z')
            cout << c;
    }
    cout << endl;

    // 第二行：空格数量
    int spaces = 0;
    for (char c : s)
    {
        if (c == ' ')
            spaces++;
    }
    cout << spaces << endl;

    return 0;
}
```

---

### 编程题 3：区间内完全平方数的个数和和

```cpp
#include <iostream>
#include <cmath>
using namespace std;

int main()
{
    int m, n;
    cin >> m >> n;

    int cnt = 0;
    long long sum = 0;

    for (int x = m; x <= n; x++)
    {
        int r = (int)(sqrt(x) + 0.0000001); // 防止浮点误差
        if (r * r == x)
        {
            cnt++;
            sum += x;
        }
    }

    cout << cnt << " " << sum << endl;
    return 0;
}
```

---

### 编程题 4：函数判断闰年并输出区间天数

```cpp
#include <iostream>
using namespace std;

bool isLeap(int year)
{
    // 闰年规则：能被4整除且不能被100整除，或能被400整除
    if ((year % 4 == 0 && year % 100 != 0) ||
        (year % 400 == 0))
        return true;
    return false;
}

int main()
{
    int y1, y2;
    cin >> y1 >> y2;

    if (y1 > y2)
    {
        int t = y1;
        y1 = y2;
        y2 = t;
    }

    for (int y = y1; y <= y2; y++)
    {
        if (isLeap(y))
            cout << 366 << endl;
        else
            cout << 365 << endl;
    }

    return 0;
}
```

---

### 编程题 5：函数重载：一维数组和二维数组求和

```cpp
#include <iostream>
using namespace std;

// 一维数组：a[0..n-1]
int getSum(int a[], int n)
{
    int s = 0;
    for (int i = 0; i < n; i++)
        s += a[i];
    return s;
}

// 二维数组：n 行 3 列
int getSum(int a[][3], int n)
{
    int s = 0;
    for (int i = 0; i < n; i++)
        for (int j = 0; j < 3; j++)
            s += a[i][j];
    return s;
}

int main()
{
    int b[3];
    for (int i = 0; i < 3; i++)
        cin >> b[i];

    int c[2][3];
    for (int i = 0; i < 2; i++)
        for (int j = 0; j < 3; j++)
            cin >> c[i][j];

    int sum1 = getSum(b, 3);
    int sum2 = getSum(c, 2);

    cout << sum1 << endl;
    cout << sum2 << endl;

    return 0;
}
```

---
