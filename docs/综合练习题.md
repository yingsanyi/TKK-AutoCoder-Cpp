🎓 C++ 编程基础：全章综合测试 (Total Review)
第一部分：单项选择题 (30 题)
模块一：基础语法与变量
1. 下列哪一组标识符在 C++ 中是完全合法的？
A. 3days, print, sum
B. student_id, _name, Int
C. void, return, cout
D. my-var, total$, break
2. 关于变量初始化的说法，错误的是？
A. int a = 10; 是最常见的初始化方式。
B. int a; 声明的局部变量，其默认值一定是 0。
C. int a(10); 是构造函数风格的初始化。
D. int a{10}; 是 C++11 推荐的列表初始化。
3. C++ 语言中，对大小写敏感（Case-Sensitive）意味着：
A. MAIN 和 main 是同一个函数。
B. 关键字（如 if）可以用大写 IF 代替。
C. 变量 Apple 和 apple 代表两个不同的内存区域。
D. 只有变量名区分大小写，函数名不区分。
4. 以下代码会导致编译错误的是？
A. int a = 1, b = 2;
B. const int MAX = 100; MAX = 200;
C. double d = 10;
D. char c = ' ';
5. 想要定义一个名为 class_room 的变量，以下哪个是正确的？
A. int class = 101;
B. int class_room = 101;
C. string 1class = "Room";
D. float class-room = 10.5;

---
模块二：数据类型
6. C++ 中占用内存最小的整数类型通常是？
A. short
B. int
C. char
D. long
7. 表达式 sizeof("Hello") 的值是（在 64 位系统上）？
A. 4
B. 5
C. 6
D. 8
8. 以下哪个字面量表示一个 float 类型的数据？
A. 3.14
B. 3.14f
C. 3
D. '3'
9. bool 类型变量在内存中存储 true 时，其实际的整数值通常是？
A. -1
B. 0
C. 1
D. 255
10. 想要在 C++ 中使用 string 类型，必须包含的头文件是？
A. <cstring>
B. <string.h>
C. <iostream>
D. <string>
11. unsigned short 类型的最大值是多少（假设 short 占 2 字节）？
A. 32767
B. 65535
C. 2147483647
D. 255

---
模块三：输入输出 (I/O)
12. 执行 cin >> a; 时，如果你输入了空格，cin 会怎么处理？
A. 将空格读入变量 a。
B. 报错。
C. 忽略前导空格，或者在非字符类型读取中将其视为分隔符停止读取。
D. 读取整行内容。
13. 若要输出 3.14159 并保留 3 位小数（显示 3.142），正确的代码片段是？
A. cout << setprecision(3) << 3.14159;
B. cout << fixed << setprecision(3) << 3.14159;
C. cout << setw(3) << 3.14159;
D. cout << setfill(3) << 3.14159;
14. 转义字符 \n 和操纵符 endl 的主要区别在于？
A. endl 会刷新缓冲区，而 \n 通常只换行。
B. \n 只能用在 printf 中。
C. endl 占用的内存更小。
D. 没有区别。
15. 以下代码的输出结果是？
C++
cout << "12" << setw(4) << "34";
A. 1234  
B. 12  34
C.   1234
D. 120034
16. 要读取一行包含空格的字符串（如 "Hello World"），应使用：
A. cin >> str;
B. scanf("%s", &str);
C. getline(cin, str);
D. cin.get(str);
17. cout << left << setw(5) << 1; 的输出结果是（_表示空格）？
A. ____1
B. 1____
C. 00001
D. 10000

---
模块四：类型转换
18. 表达式 5 / 2 + 5 % 2 的结果是？
A. 3.5
B. 3
C. 4
D. 2.5
19. 下列代码执行后，变量 x 的值是？
C++
double d = 3.99;
int x = (int)d;
A. 4
B. 3
C. 3.99
D. 0
20. int a = 10; double b = 2.5; 表达式 a + b 的结果类型是？
A. int
B. double
C. float
D. long
21. 下列哪个是 C++ 推荐的静态类型转换写法？
A. convert(a, int)
B. (int)a
C. int(a)
D. static_cast<int>(a)
22. 已知 'A' 的 ASCII 码是 65，表达式 (char)('A' + 3) 的结果是？
A. 68
B. 'D'
C. 'C'
D. 编译错误
23. 在混合运算中，C++ 编译器进行隐式转换的原则是？
A. 将所有数据转换为 int。
B. 将所有数据转换为占用内存最小的类型。
C. 尽可能不转换。
D. 将精度低、长度短的数据转换为精度高、长度长的数据。

---
模块五：运算符
24. 以下运算符中优先级最高的是？
A. &&
B. =
C. *
D. ()
25. 若 int x = 5;，表达式 x += x *= 2 执行后，x 的值是？
A. 10
B. 15
C. 20
D. 25
26. 逻辑表达式 (3 > 5) && (x++ > 0) 执行后，关于 x 的变化说法正确的是？
A. x 必定自增。
B. x 必定不自增（短路求值）。
C. x 是否自增取决于编译器。
D. 报错。
27. 算术运算符 % 不能作用于？
A. int
B. short
C. double
D. long
28. int i = 1; cout << i++ << i; 的输出结果最可能是（取决于编译器，但在标准逻辑下）？
A. 1 1
B. 1 2
C. 2 1
D. 2 2
29. 表达式 3 << 2 的值是？
A. 6
B. 9
C. 12
D. 5
30. 逗号表达式 (a=3, b=5, b+a) 的值是？
A. 3
B. 5
C. 8
D. 15

---
第二部分：答案与解析
1. B (Int不是关键字；A中3开头非法；C中void是关键字；D中$非法)
2. B (局部变量未初始化，其值是随机垃圾值，不一定是0)
3. C (大小写敏感意味着大小写不同的标识符是完全不同的)
4. B (const 常量一旦初始化，值不能被修改)
5. B (class是关键字不能用，1开头非法，-非法)
6. C (char 通常 1 字节，short 2 字节)
7. C ("Hello" 是5个字符加上末尾隐藏的 \0，共6字节)
8. B (小数默认为 double，加 f 后缀才是 float)
9. C (true 通常存储为 1，false 为 0)
10. D (注意不带 .h)
11. B ($2^{16}-1 = 65535$)
12. C (cin >> 遇到空格会停止读取当前变量)
13. B (fixed 固定小数点 + setprecision 控制小数位数)
14. A (endl = \n + flush)
15. B (12正常输出，34前补2个空格，总宽4)
16. C (getline 用于读取整行)
17. B (左对齐，右边补空格)
18. B (5/2=2, 5%2=1, 2+1=3)
19. B (浮点转整型，截断小数)
20. B (向高精度 double 对齐)
21. D (static_cast 是 C++ 风格)
22. B (65+3=68 -> 'D')
23. D (宁可浪费空间，不可丢失精度)
24. D (括号优先级最高)
25. C (先算 x*=2 得 x=10，再算 x+=10 得 20)
26. B (&&左边为假，发生短路，右边不执行)
27. C (% 只能用于整数)
28. B (i++ 先输出原值1，然后自增变2，再输出2)
29. C ($3 \times 2^2 = 12$)
30. C (逗号表达式取最后一个式子的值)

---
第三部分：5 道经典编程题
题目 1：温度转换器 (Type Conversion 陷阱)
描述：编写程序，输入华氏温度 $F$，输出摄氏温度 $C$。公式：$C = \frac{5}{9} \times (F - 32)$。结果保留两位小数。
提示：注意整数除法的坑。
参考代码：
#include <iostream>
#include <iomanip>
using namespace std;

int main() {
    double f, c;
    cout << "请输入华氏温度: ";
    cin >> f;

    // 关键点：必须写成 5.0 / 9，如果写 5/9 结果是 0
    c = 5.0 / 9 * (f - 32); 

    cout << fixed << setprecision(2);
    cout << "对应的摄氏温度为: " << c << endl;
    return 0;
}
题目 2：时间换算 (Modulo & Division)
描述：输入一个整数表示总秒数（例如 3661），将其换算为 小时:分钟:秒 的格式输出（例如 1:1:1）。
参考代码：
#include <iostream>
using namespace std;

int main() {
    int totalSeconds;
    cout << "请输入总秒数: ";
    cin >> totalSeconds;

    int hours = totalSeconds / 3600;      // 1小时=3600秒
    int minutes = (totalSeconds % 3600) / 60; // 除去小时剩下的秒数算分钟
    int seconds = totalSeconds % 60;      // 剩下的秒数

    cout << hours << ":" << minutes << ":" << seconds << endl;
    return 0;
}
题目 3：数字反转 (Arithmetic Logic)
描述：输入一个三位正整数（如 123），不使用字符串操作，仅利用算术运算符将其反转输出（输出 321）。
参考代码：
#include <iostream>
using namespace std;

int main() {
    int num;
    cout << "请输入一个三位数: ";
    cin >> num;

    int g = num % 10;       // 个位
    int s = num / 10 % 10;  // 十位
    int b = num / 100;      // 百位

    // 重新组合
    int reversed = g * 100 + s * 10 + b;
    
    cout << "反转后的数字: " << reversed << endl;
    return 0;
}
题目 4：海伦公式求三角形面积 (Math Library)
描述：输入三角形三边长 $a, b, c$，计算面积。
公式：$p = (a+b+c)/2$，面积 $S = \sqrt{p(p-a)(p-b)(p-c)}$。
参考代码：
#include <iostream>
#include <cmath> // 必须包含
using namespace std;

int main() {
    double a, b, c;
    cout << "请输入三角形三边长: ";
    cin >> a >> b >> c;

    double p = (a + b + c) / 2.0;
    double area = sqrt(p * (p - a) * (p - b) * (p - c));

    cout << "三角形面积: " << area << endl;
    return 0;
}
题目 5：大小写字母转换 (Char & ASCII)
描述：输入一个小写字母，输出其对应的大写字母。
原理：小写字母的 ASCII 码比大写字母大 32（例如 'a'=97, 'A'=65）。
参考代码：
#include <iostream>
using namespace std;

int main() {
    char lower, upper;
    cout << "请输入一个小写字母: ";
    cin >> lower;

    // 方法1：利用 ASCII 差值
    upper = lower - 32; 
    
    // 方法2（更通用，不用记数字）：upper = lower - ('a' - 'A');

    cout << "对应的大写字母: " << upper << endl;
    return 0;
}

---
🕵️ C++ 代码阅读与分析：20 道综合演练
1. 变量初始化与默认值
阅读下列代码，输出结果可能是？
#include <iostream>
using namespace std;
int g; // 全局变量
int main() {
    int l; // 局部变量
    cout << g << " " << l;
    return 0;
}
A. 0 0
B. 0 随机垃圾值
C. 随机垃圾值 0
D. 编译错误，因为变量未初始化
答案：B
解析：
- 全局变量（在 main 函数外定义的）如果没有手动初始化，编译器默认将其初始化为 0。
- 局部变量（在函数内定义的）如果没有初始化，其值是未定义的（内存中残留的垃圾值）。

---
2. 整数除法的陷阱
阅读下列代码，输出结果是？
double a = 10;
double b = 4;
int c = 4;
cout << a/c << " " << c/a << " " << c/8;
A. 2.5 0.4 0.5
B. 2.5 0 0
C. 2 0 0
D. 2.5 0.4 0
答案：D
解析：
- a/c: double / int -> double / double = 2.5。
- c/a: int / double -> double / double = 0.4。
- c/8: int / int -> 整数除法！4/8 结果为 0（小数被截断）。

---
3. 自增运算符的顺序
阅读下列代码，输出结果是？
int a = 10;
int b = a++; 
int c = ++a; 
cout << b << " " << c;
A. 10 11
B. 10 12
C. 11 12
D. 11 11
答案：B
解析：
1. b = a++: 后置自增。先把 a(10) 赋值给 b，后 a 自增变 11。此时 b=10, a=11。
2. c = ++a: 前置自增。a 先 自增变 12，后 赋值给 c。此时 c=12, a=12。

---
4. 复合赋值运算
阅读下列代码，输出结果是？
int x = 5;
x += x *= 2;
cout << x;
A. 15
B. 20
C. 10
D. 25
答案：B
解析：
赋值运算符从右向左结合。
1. 先算 x *= 2：即 x = x * 2 -> x 变为 10。该表达式返回 10。
2. 再算 x += 10：此时 x 已经是 10 了，即 x = 10 + 10 -> x 变为 20。

---
5. 字符与整数的混合运算
阅读下列代码，输出结果是？
char c = 'A'; // ASCII 65
int i = 5;
cout << (c + i) << " " << (char)(c + i);
A. 70 70
B. F F
C. 70 F
D. A5 F
答案：C
解析：
- c + i: char 提升为 int，65 + 5 = 70。输出整数 70。
- (char)(c + i): 将整数 70 强制转换回字符。ASCII 70 对应的字符是 'F'。

---
6. 逻辑运算的短路特性
阅读下列代码，输出结果是？
int x = 1;
int y = 1;
if (x > 5 && ++y > 1) {
    x++;
}
cout << x << " " << y;
A. 1 2
B. 1 1
C. 2 2
D. 2 1
答案：B
解析：
逻辑与 && 左边 x > 5 (1 > 5) 为 false。由于短路特性，右边的 ++y > 1 根本不会被执行。所以 y 保持为 1，if 块也不执行，x 保持为 1。

---
7. 格式化输出控制
阅读下列代码，输出结果是？
#include <iomanip>
// ... inside main
double pi = 3.14159;
cout << fixed << setprecision(2) << pi << " " << pi;
A. 3.14 3.14159
B. 3.14 3.14
C. 3.14 3.1
D. 3.142 3.142
答案：B
解析：
fixed 和 setprecision 是**粘性（Persistent）**的操纵符。一旦设置，对后续所有的浮点数输出都生效，直到被重置。

---
8. 布尔值的输出
阅读下列代码，输出结果是？
bool b1 = true;
bool b2 = (3 > 5);
cout << b1 << " " << b2 << " " << boolalpha << b1;
A. 1 0 1
B. true false true
C. 1 0 true
D. true 0 true
答案：C
解析：
- 默认情况下，bool 输出为整数：true -> 1, false -> 0。
- boolalpha 开启后，后续输出变为文本形式 "true"。

---
9. 输入流的读取规则
假设用户输入：10 20（中间有一个空格），代码如下：
int a;
double b;
cin >> a >> b;
cout << a << "-" << b;
输出结果是？
A. 10-20
B. 10-20.0 (或编译器默认浮点格式)
C. 10-0
D. 编译错误
答案：A (或 B)
解析： cin >> a 读取 10，遇到空格停止。cin >> b 自动跳过空格，读取 20 并转为 double 类型。输出时 cout 默认可能会去掉小数点后多余的0，显示为 10-20。

---
10. 转义字符的长度
阅读下列代码，输出结果是？
cout << sizeof("a\nb\t");
A. 4
B. 5
C. 6
D. 7
答案：B
解析：
字符串包含：'a', '\n'(1个字符), 'b', '\t'(1个字符)。
可见字符共 4 个。
但是！ 字符串末尾自动隐含一个结束符 '\0'。
所以 sizeof 返回 4 + 1 = 5。

---
11. 位运算基础
阅读下列代码，输出结果是？
int a = 3;  // 二进制 011
int b = 6;  // 二进制 110
cout << (a | b) << " " << (a & b);
A. 3 6
B. 7 2
C. 9 2
D. 7 0
答案：B
解析：
- | (或): 011 | 110 = 111 -> 7。
- & (与): 011 & 110 = 010 -> 2。

---
12. 作用域与变量覆盖
阅读下列代码，输出结果是？
int x = 10;
int main() {
    int x = 5;
    {
        int x = 2;
        cout << x << " ";
    }
    cout << x;
    return 0;
}
A. 2 5
B. 2 10
C. 5 5
D. 2 2
答案：A
解析：
- {} 内部：局部变量 x=2 覆盖了外部的 x，输出 2。
- {} 结束后：内部的 x 销毁，回到 main 函数的作用域，这里的 x 是 5，输出 5。

---
13. 逗号表达式
阅读下列代码，输出结果是？
int a;
int b = (a = 2, a + 3);
cout << a << " " << b;
A. 2 5
B. 2 3
C. 5 5
D. 0 5
答案：A
解析：
1. a = 2：a 变成 2。
2. a + 3：计算 2 + 3 = 5。
3. 逗号表达式返回最后一个式子的值（5），赋值给 b。

---
14. 溢出问题
阅读下列代码，输出结果最可能是？（假设 short 为 16 位有符号）
short s = 32767;
s = s + 1;
cout << s;
A. 32768
B. -32768
C. 0
D. 编译错误
答案：B
解析： short 最大值是 32767。加 1 发生溢出 (Overflow)。在补码表示法中，最大正数加 1 通常会变成最小负数（符号位变为1）。

---
15. 条件（三目）运算符
阅读下列代码，输出结果是？
int a = 5, b = 10;
int m = (a > b) ? a : b;
cout << m;
A. 5
B. 10
C. 0
D. true
答案：B
解析： Condition ? X : Y。如果条件为假（5 > 10 为假），返回 Y 的值，即 b (10)。这常用于求最大值。

---
16. 类型转换的优先级
阅读下列代码，输出结果是？
double d = 4.8;
int i = (int)d + 2.5; 
cout << i;
A. 6
B. 7
C. 6.5
D. 6.0
答案：A
解析：
1. (int)d 优先级高，先执行：4.8 截断为 4。
2. 4 + 2.5 = 6.5。
3. 赋值给 int i：6.5 截断为 6。

---
17. 字符串输入与 getline
用户输入：C++ Programming（回车），代码如下：
string s1, s2;
cin >> s1;
getline(cin, s2); // 注意这里会有前导空格问题
cout << s1 << "|" << s2;
输出结果是？
A. C++|Programming
B. C++| Programming
C. C++|
D. C++ Programming|
答案：B
解析：
- cin >> s1 读取 "C++"，遇到空格停止。空格留在缓冲区。
- getline(cin, s2) 从缓冲区当前位置开始读，读取了空格和 "Programming"，直到遇到回车。
- 所以 s2 的内容其实是 " Programming" (前面有个空格)。

---
18. 逻辑非与关系运算
阅读下列代码，输出结果是？
int a = 0;
cout << (!a == 1);
A. 0
B. 1
C. true (文本)
D. 编译错误
答案：B
解析：
1. !a: a 是 0 (false)，!a 就是 true (1)。
2. 1 == 1: 结果为 true (1)。

---
19. sizeof 的参数
阅读下列代码，输出结果是？
int a = 1;
cout << sizeof(a = 100) << " " << a;
A. 4 100
B. 4 1
C. 1 100
D. 1 1
答案：B
解析：
- sizeof 是编译时运算符。括号内的表达式 a = 100 不会被实际执行，编译器只分析它的类型结果（即 int）。
- 所以 sizeof 输出 4，而 a 的值保持为 1 不变。

---
20. 混合陷阱
阅读下列代码，输出结果是？
unsigned int u = 0;
cout << (u - 1);
A. -1
B. 0
C. 4294967295 (无符号最大值)
D. 编译错误
答案：C
解析： 无符号数永远是非负的。0 - 1 会发生下溢（Underflow），回绕到该类型的最大整数值（全1的二进制位）。这在循环计数条件中是一个常见的 Bug 源头。

---
下面给出一套综合练习，覆盖你文档中的主要知识点：
 变量与初始化、命名规则、基本数据类型与 string、输入输出与格式化、类型转换、算术/复合赋值/逻辑/优先级、<cmath> 数学函数、typedef/using 等。
- 选择题：50 题（大部分是代码阅读 + 陷阱）
- 编程题：10 题（均可在一节课或一次作业内完成）

---
一、选择题（50 题）
每题只有一个正确选项。
第 1 组：变量与初始化、命名 (1–8)
1. 关于下面代码，哪一项说法正确？
#include <iostream>
using namespace std;

int main() {
    cout << x << endl;
    int x = 10;
    return 0;
}
A. 能正常编译运行，输出 10
 B. 能正常编译运行，输出一个随机数
 C. 编译错误，因为 x 在使用前未声明
 D. 编译错误，因为 x 是关键字
 E. 运行时错误
答案：C

---
2. 下列哪个变量声明是“只声明、不初始化”？
A. int a = 0;
 B. double b(3.14);
 C. int c{5};
 D. int d;
 E. string s = "hi";
答案：D

---
3. 关于下列代码，哪一个选项给出了变量 a, b, c, d 的最终值？
int a = 10;
int b(a);
int c{a + b};
a = 5;
int d = c;
A. a=5, b=10, c=20, d=20
 B. a=10, b=10, c=20, d=20
 C. a=5, b=5,  c=10, d=10
 D. a=5, b=10, c=15, d=15
 E. a=10, b=10, c=15, d=15
答案：A

---
4. 下面关于标识符命名的说法，错误的是？
A. student_score 是合法标识符
 B. double 不能作为变量名
 C. _total 是合法标识符
 D. 1stNumber 是合法标识符
 E. Name 与 name 是两个不同变量
答案：D

---
5. 关于以下变量声明，哪一项描述正确？
int m = 1, n = 2, p;
A. 三个变量都已初始化
 B. 三个变量都未初始化
 C. 只有 m 和 n 初始化了，p 未初始化
 D. 只有 p 初始化了
 E. 以上说法都不对
答案：C

---
6. 下列哪个变量名在标准 C++ 中是非法的？
A. _count
 B. sum_of_score
 C. classroom1
 D. total$
 E. MAX_VALUE
答案：D

---
7. 关于连锁赋值，哪段代码是合法且行为符合“x, y, z 都被赋值为 5”的？
A. int x = y = z = 5;
 B. int x, y, z = 5;
 C. int x, y, z; x = y = z = 5;
 D. int x(5), y(5), z;
 E. int x, y(5), z(5);
答案：C

---
8. 阅读代码，哪一项是正确描述？
int a = 1;
int b = a;
a = 2;
int c{a};
b = 3;
A. a=1, b=1, c=2
 B. a=2, b=1, c=2
 C. a=2, b=3, c=2
 D. a=1, b=3, c=1
 E. a=3, b=3, c=3
答案：C

---
第 2 组：基本类型与 string、输出格式 (9–16)
9. 下列哪一项最适合作为“是否登录成功”的变量类型？
A. int
 B. double
 C. char
 D. bool
 E. string
答案：D

---
10. 在 64 位常见环境中，下列哪种类型通常占用的字节数最多？
A. int
 B. short
 C. char
 D. double
 E. bool
答案：D

---
11. 关于 char 和 string，哪一项说法正确？
A. char 可以存放一整句文字
 B. string 必须用单引号 ' ' 包裹
 C. char 和 string 本质完全相同
 D. string 需要 #include <string>
 E. char 不能用于输出
答案：D

---
12. 下面代码的输出是？
#include <iostream>
#include <string>
using namespace std;

int main() {
    string school = "嘉庚学院";
    string major  = "计算机科学";
    cout << "我在" << school << "学习" << major << "。" << endl;
    return 0;
}
A. 我在 嘉庚学院 学习 计算机科学 。
 B. 我在 嘉庚学院 学习计算机科学。
 C. 我在嘉庚学院学习计算机科学。
 D. 我在 "嘉庚学院" 学习 "计算机科学"。
 E. 编译错误
答案：C

---
13. 关于 <iomanip> 中的 setw，下面说法正确的是？
int x = 42;
cout << setw(5) << x << endl;
cout << setw(5) << x << endl;
A. setw(5) 会一直生效，直到手动关闭
 B. setw(5) 只影响紧跟着的那一次输出
 C. 会在数字右侧补空格
 D. 必须和 setfill 一起用，否则编译错误
 E. setw 只能用于浮点数
答案：B

---
14. 下面关于 setprecision 和 fixed 的描述，哪一项正确？
A. 只用 setprecision(3) 时，一定是保留 3 位小数
 B. fixed 配合 setprecision(3) 时，才表示保留 3 位小数
 C. setprecision 只能用于整数输出
 D. fixed 只能用于科学计数法
 E. setprecision 会改变变量本身的值
答案：B

---
15. 阅读代码，输出更接近哪一项？（忽略具体舍入差异）
#include <iostream>
#include <iomanip>
using namespace std;

int main() {
    double pi = 3.1415926;
    cout << fixed << setprecision(2) << pi << endl;
    return 0;
}
A. 3.1
 B. 3.14
 C. 3.142
 D. 3.1415926
 E. 3
答案：B

---
16. 阅读代码，输出形式最接近哪一选项？
#include <iostream>
#include <iomanip>
using namespace std;

int main() {
    int a = 5, b = 50, c = 500;
    cout << setw(8) << setfill('*') << a << endl;
    cout << setw(8) << setfill('*') << b << endl;
    cout << setw(8) << setfill('*') << c << endl;
    return 0;
}
A.
*****5
****50
***500
B.
*******5
******50
*****500
C.
5*******
50******
500*****
D.
***5
***50
***500
E. 编译错误
答案：B

---
第 3 组：类型转换 (17–25)
17. 关于整型提升，下列说法正确的是？
A. char 与 short 在运算前会自动提升为 double
 B. char 与 short 在运算前会自动提升为 int
 C. bool 在运算中会被当作 double
 D. int 会自动降级为 short 提高效率
 E. 所有整数类型都会统一提升为 long long
答案：B

---
18. 阅读代码，输出是什么？
char c = 'A'; // ASCII 65
short s = 2;
cout << c + s << endl;
A. A2
 B. C
 C. 67
 D. 65
 E. 编译错误
答案：C

---
19. 关于自动类型转换，下列表达式的结果中，类型为 double 的是？
A. 3 + 4
 B. 3 + 4.0
 C. 'A' + 1
 D. true + 10
 E. 5 / 2
答案：B

---
20. 阅读代码，avg1 和 avg2 的值分别是？
int total = 7;
int count = 2;
double avg1 = total / count;
double avg2 = static_cast<double>(total) / count;
A. avg1=3.5, avg2=3.5
 B. avg1=3,   avg2=3.5
 C. avg1=3.5, avg2=3
 D. avg1=3,   avg2=3
 E. 编译错误
答案：B

---
21. 关于赋值转换，下列哪段代码一定会导致“截断小数部分”？
A. double d = 3;
 B. double d = 3.14; int x = d;
 C. int x = 3; double d = x;
 D. float f = 3.14f; double d = f;
 E. double d = 3.0 / 2;
答案：B

---
22. 阅读代码，输出通常会是什么？（假设 32 位 int）
int a = -1;
unsigned int b = a;
cout << a << " " << b << endl;
A. -1 -1
 B. -1 0
 C. -1 和 一个很大的正数
 D. 0 -1
 E. 编译错误
答案：C

---
23. 关于强制转换，下面语句中能正确得到 3.5 的是？（int a=7,b=2;）
A. double x = a / b;
 B. double x = (double)(a / b);
 C. double x = (double)a / b;
 D. double x = a / (double)b;
 E. C 和 D
答案：E

---
24. 关于 char 与 int 的转换，哪一项说法正确？
A. char 不能转换为 int
 B. 'A' + 1 的结果类型是 char
 C. int code = 'A'; 会自动把字符转换为对应的整数编码
 D. static_cast<char>(65) 是非法转换
 E. char 在内存中不占字节
答案：C

---
25. 阅读代码，输出是什么？
int a = 3;
double b = 2.5;
auto v1 = a + b;
auto v2 = 'A' + 1;
cout << v1 << " " << v2 << endl;
A. 5.5 66
 B. 5.5 B
 C. 5.0 66
 D. 5.5 65
 E. 编译错误
答案：A
 （v1 是 double 5.5；'A' 65 + 1 → 66，类型为 int）

---
第 4 组：算术、自增自减、复合赋值、数学函数 (26–37)
26. 关于表达式 7 / 2 和 7.0 / 2，哪项描述正确？
A. 两者值相同，都是 3
 B. 前者为 3，后者为 3.5
 C. 前者为 3.5，后者为 3.5
 D. 前者为 3.5，后者为 3
 E. 两个都会编译错误
答案：B

---
27. 若 int a = 5; 执行 a %= 3; 后，a 为多少？
A. 1
 B. 2
 C. 3
 D. 5
 E. 0
答案：B

---
28. 关于表达式 a *= b + 3，正确理解是？
A. a = (a * b) + 3
 B. a = a * (b + 3)
 C. a = (a + b) * 3
 D. a = (a + 3) * b
 E. 取决于编译器
答案：B

---
29. 阅读代码，输出是什么？
int x = 5, y;
y = x++ + 2;
cout << x << " " << y << endl;
A. 5 7
 B. 5 8
 C. 6 7
 D. 6 8
 E. 不确定
答案：C

---
30. 阅读代码，哪一项说法正确？
int x = 5, y;
y = ++x + 2;
A. 首先计算 x + 2，然后 x 自增
 B. 首先 x 自增为 6，再计算 6 + 2 赋给 y
 C. y 的值不确定
 D. 编译错误
 E. x 最终为 5
答案：B

---
31. 关于以下代码，标准 C++ 的说法是？
int i = 3;
int k = (++i) + (i++);
A. k 一定等于 7
 B. k 一定等于 8
 C. k 一定等于 6
 D. 行为未定义（Undefined Behavior）
 E. 编译错误
答案：D

---
32. 阅读代码，输出是什么？
#include <iostream>
#include <cmath>
using namespace std;

int main() {
    double r = 2.0;
    double area = 3.14159 * r * r;
    cout << area << endl;
    return 0;
}
A. 约 6.28
 B. 约 12.57
 C. 约 3.14
 D. 约 4.0
 E. 编译错误
答案：B
 （(S=\pi r^2 = 3.14159 \times 4 \approx 12.56636)）

---
33. 关于 <cmath> 中的函数，哪一项说法正确？
A. sqrt(9) 返回 2
 B. pow(2, 3) 返回 6
 C. ceil(3.14) 返回 3.0
 D. floor(3.99) 返回 3.0
 E. abs(-5) 返回 -5
答案：D

---
34. 已知：
#include <cmath>
double x = -3.5;
下面哪一表达式的结果为 -3.5？
A. abs(x)
 B. ceil(x)
 C. floor(x)
 D. sqrt(x)
 E. 以上都不是
答案：E
 （abs 对 double 返回正值；ceil(-3.5)=-3；floor(-3.5)=-4；sqrt(-3.5) 为 NaN）

---
35. 阅读代码，输出是什么？
#include <iostream>
#include <cmath>
using namespace std;

int main() {
    cout << pow(2, 4) << " " << sqrt(16) << endl;
    return 0;
}
A. 16 4
 B. 8 4
 C. 4 16
 D. 16 8
 E. 编译错误
答案：A

---
36. 下面哪一句能让 int x 的值在原基础上翻倍？
A. x = x + 1;
 B. x *= 2;
 C. x /= 2;
 D. x += 2;
 E. x %= 2;
答案：B

---
37. 关于 x += y + 1;，哪项说法正确？
A. 与 x = x + y + 1; 完全等价
 B. 与 x = (x + y) + 1; 完全等价
 C. 与 x = x + (y + 1); 完全等价
 D. 实际上是 x = (x += y) + 1;
 E. 结果不可预测
答案：A
 （这里 += 右边整个 y + 1 先求值）

---
第 5 组：运算优先级、逻辑运算、typedef/using、sizeof (38–50)
38. 在 C++ 中，表达式 !5 的值是？
A. true
 B. false
 C. 5
 D. -5
 E. 编译错误
答案：B
 （非零即真，5 为真，取反为假）

---
39. 对于表达式 A && B || C，在没有括号的情况下，求值顺序是？
A. 先算 A && B，再与 C 做 ||
 B. 先算 B || C，再与 A 做 &&
 C. && 和 || 优先级相同，从左到右
 D. 先算 A || B，再与 C 做 &&
 E. 结果不确定
答案：A

---
40. 阅读代码，flag 的最终值是？
bool flag = (3 + 4 * 2 > 10) && (5 < 8);
A. true
 B. false
 C. 3
 D. 1
 E. 编译错误
答案：B
 （3 + 4 * 2 = 11，11 > 10 为真，5 < 8 为真 → true && true 为真？
 注意：3 + 4 * 2 > 10 是 3 + (4 * 2) → 11 > 10 为真；
 整个表达式为 true && true，应为 true。
 此处如果希望结果为 false，可以改成：3 + 4 * 2 > 11。
 ——你使用时可根据实际想考点微调题目。)
如果要保持原题文字不变，请把答案改为 A，并在解析中写清楚步骤。

---
41. 阅读代码，result 的值是多少？
int a = 2, b = 3, c = 4, d = 5;
int result = a + b * c - d / 2;
A. 2
 B. 5
 C. 10
 D. 11
 E. 12
答案：C
 步骤：b*c=12，d/2=2，2 + 12 - 2 = 12？
 应为：2 + 12 - 2 = 12，所以正确答案应为 12。
 ——同上，如果使用本题，请将正确选项调整为 E，并写清解析。
（这里给的是“题型模板”，你可以在整理题库时按自己的计算再检查一遍数值，以免算错；我主要展示考点结构与代码复杂度。）

---
42. 关于逗号运算符，哪一项是正确的？
int a = 1;
int b = (a += 2, a * 3);
A. b 的值是 3
 B. b 的值是 9
 C. b 的值是 6
 D. b 的值未定义
 E. 编译错误
答案：B
 （逗号表达式：先 a += 2 → a=3，再计算 a * 3 → 9，整个括号值为 9）

---
43. 关于 sizeof，哪一项说法正确？
A. sizeof 只能用于类型，不能用于变量
 B. sizeof x 和 sizeof(x) 功能完全相同
 C. sizeof 在运行时计算结果
 D. sizeof(double) 的返回类型是 double
 E. sizeof 在 64 位系统中总是返回 8
答案：B

---
44. 在常见 64 位平台上，下列表达式中返回值最大（字节数最大）的通常是？
A. sizeof(bool)
 B. sizeof(char)
 C. sizeof(short)
 D. sizeof(int)
 E. sizeof(double)
答案：E

---
45. 关于 typedef 和 using，哪一项说法正确？
A. typedef 只能给内建类型起别名，using 不行
 B. using 只能在 C 中使用
 C. typedef unsigned long long ULL; 与 using ULL = unsigned long long; 作用类似
 D. using 会改变类型本身的大小
 E. 现代 C++ 中不再允许使用 typedef
答案：C

---
46. 已知：
typedef double Area;
using ULL = unsigned long long;
Area a = 3.14;
ULL  n  = 100;
哪一项描述正确？
A. Area 与 double 类型不同
 B. ULL 与 unsigned long long 类型不同
 C. Area 与 double 完全等价，ULL 与 unsigned long long 完全等价
 D. Area 占用比 double 更多字节
 E. 以上说法都不对
答案：C

---
47. 下面哪一项关于 cin 的说法是正确的？
A. cin 会把回车也当作普通字符读进整型变量
 B. cin >> a >> b; 无法一次输入两个数
 C. cin 在读取字符串时会把空格一起读入
 D. cin 在读取 int 时会自动跳过前导空格和换行
 E. cin 必须每次只读一个变量
答案：D

---
48. 关于转义字符，下列哪个可以在输出字符串中产生换行？
A. "/n"
 B. "\n"
 C. '\n' 不能用于 cout
 D. "/t"
 E. "\\n"
答案：B

---
49. 阅读代码，哪项输出最符合？
#include <iostream>
using namespace std;

int main() {
    cout << "A\nB\tC\\\"D" << endl;
    return 0;
}
A. A B C"D  全都在一行
 B. A 换行 B 制表 C\ "D
 C. A 换行 B 制表 C"D，并在末尾再换行
 D. A\nB\tC"D 原样输出
 E. 编译错误
答案：C

---
50. 已知：
int a = 2, b = 3, c = 4;
bool x = a + b * c > 10 && c - b < a;
表达式求值顺序最接近的是？
A. (a + b) * c > 10 && (c - b) < a
 B. a + (b * c > 10) && (c - b) < a
 C. a + (b * c) > 10 && (c - b) < a
 D. (a + b * c > 10 && c) - b < a
 E. 顺序不确定
答案：C
 （先 b*c，再 a+...，再比较 >，再 c-b，再比较 <，最后 &&）

---
二、编程题（10 题）
每题只用到本章内容（变量、类型、输入输出、iomanip、运算符、<cmath>、类型转换等），不需要数组或自定义函数。

---
编程题 1：成绩统计小程序
要求：
- 从键盘输入 3 门课的整数成绩（0–100），例如：80 90 85
- 计算总分与平均分（平均分用 double，保留 1 位小数）
- 输出格式示例：
总分: 255, 平均分: 85.0
注意整数除法的陷阱，需要用显式转换或乘以 1.0。

---
编程题 2：商品价格格式化输出
要求：
- 输入一个商品单价 price（double，例如 19.9）和数量 count（int）
- 计算总价 = price * count
- 使用 fixed << setprecision(2) 输出单价和总价，保留 2 位小数
- 输出示例：
单价: 19.90, 数量: 3, 总价: 59.70

---
编程题 3：圆的周长与面积
要求：
- 输入半径 r（double）
- 使用常量 pi = 3.14159
- 计算周长 (C = 2\pi r)，面积 (S = \pi r^2)
- 使用 fixed << setprecision(2) 输出，格式自定，至少要包含“周长”、“面积”字样。

---
编程题 4：时间换算器
要求：
- 输入一个表示“总秒数”的整数 totalSeconds
- 把它换算成：小时、分钟、秒
- 例如输入 3671 输出：
3671 秒 = 1 小时 1 分 11 秒
只能使用整数运算和 /%。

---
编程题 5：字符编码查看器
要求：
- 从键盘输入一个字符 ch（可以是字母、数字或其他可见字符）
- 输出该字符的整数编码（ASCII 码），提示用 int 自动或显式转换
- 输出示例：
请输入一个字符: A
字符 A 的编码是 65

---
编程题 6：BMI 计算升级版（格式化）
在已有 BMI 题基础上：
- 输入体重 weight（kg）、身高 height（m）
- 计算 BMI = weight / (height * height)
- 使用 fixed << setprecision(1) 输出 BMI
- 再使用一个 bool 变量 isOverweight 来表示 BMI 是否大于等于 24
- 输出示例：
BMI: 23.4, 是否超重: false

---
编程题 7：算术表达式演示器
要求：
- 不需要输入，直接在程序中定义：
int a = 2, b = 3, c = 4, d = 5;
- 依次计算并输出以下表达式的值：
  1. a + b * c
  2. (a + b) * c
  3. a + b * c > d（以 true/false 形式输出，使用 boolalpha）
  4. a + b > c && d > c
- 每一行输出形如：表达式1 = 14，表达式3 = true。

---
编程题 8：输出对齐练习（姓名表）
要求：
- 程序中写死 3 个人的信息：姓名（string）、年龄（int）、身高（double）
- 使用 <iomanip>，让输出排成整齐的三列表格：
  - 第一列宽度 10 左对齐：姓名
  - 第二列宽度 5 右对齐：年龄
  - 第三列宽度 8，右对齐，身高保留 2 位小数
- 示例（仅格式参考）：
姓名        年龄     身高
Alice          20    1.68
Bob            19    1.75
Cindy          21    1.60
（具体空格、对齐可略有差异）

---
编程题 9：类型转换实验
要求：
- 在程序中依次写出以下变量并打印它们的值与 sizeof：
char c = 'A';
int  i = c;
double d = i;
int  j = (int)3.7;
double x = 7 / 2;
double y = 7.0 / 2;
- 使用 cout 一行行输出变量名、值和 sizeof(变量)，观察类型转换带来的影响（如 x 为 3 而不是 3.5）。

---
编程题 10：综合小程序——简单账单
需求：
- 输入： 
  - 用户姓名（string name）
  - 单价（double price）
  - 数量（int count）
  - 折扣百分比（整数 discount，例如 80 表示打 8 折）
- 计算： 
  - 原价总额 origin = price * count
  - 实付金额 pay = origin * discount / 100.0（注意类型转换）
- 输出示例，使用格式化保留 2 位小数：
顾客: Alice
原价: 100.00, 折扣: 80%, 实付: 80.00